#!/bin/zsh

# export GOPATH=$HOME/.local/opt/go_data
# export KEYBASE_SYSTEMD=0

# if [[ ! $DISPLAY && $XDG_VTNR -eq 1 ]]; then
#   exec startx
# fi

if [ `uname` = Darwin -a ! -f /tmp/userconf_inited_boot ]; then
  echo "please enable pf && run ssh-add"
fi

# #export NODE_PATH=`npm -g root`
# eval "$(npm completion 2>/dev/null)"

# export GOPATH=$HOME/.local/go

# export PIP_DOWNLOAD_CACHE=$HOME/.pip/cache
# if [ ! -d $PIP_DOWNLOAD_CACHE ]; then
#   mkdir -p $PIP_DOWNLOAD_CACHE
# fi

# export RUBY_BUILD_SKIP_MIRROR=1
# export RUBY_BUILD_CACHE_PATH=$HOME/.rbenv/cache
# if [ ! -d $RUBY_BUILD_CACHE_PATH ]; then
#   mkdir -p $RUBY_BUILD_CACHE_PATH
# fi

# path=(
#   "$HOME/.local/go/bin"
#   $path
# )
# export PATH

# export GIT_PROXY_HOST='127.0.0.1'
# export GIT_PROXY_PORT='7071'

# export http_proxy="http://127.0.0.1:7051"
# export https_proxy="$http_proxy"

# export GOPATH=$HOME/.local/go
# export GOROOT=/usr/lib/go

# export PYTHONDOCS=/usr/share/doc/python3.6/html/

# export HOMEBREW_AUTO_UPDATE_SECS=999999999
# export HOMEBREW_NO_GITHUB_API=1
# export HOMEBREW_NO_ANALYTICS=1

source $HOME/.dotfiles/zshrc
source /usr/share/zsh/plugins/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh

active_asdf() {
  source $HOME/.asdf/asdf.sh
  source $HOME/.asdf/completions/asdf.bash
}

active_ghcup() {
  export PATH=$HOME/.ghcu/bin:$PATH
}

active_nix() {
  source $HOMe/.nix-profile/etc/profile.d/nix.sh
}

pgcli() { $HOME/.local/opt/pyenv/pgcli/bin/pgcli $@ }
mycli() { $HOME/.local/opt/pyenv/pgcli/bin/mycli $@ }
litecli() { $HOME/.local/opt/pyenv/pgcli/bin/litecli $@ }

#alias mc=mcli

export _JAVA_OPTIONS=" -XX:ActiveProcessorCount=4 $_JAVA_OPTIONS"
export ANDROID_HOME=~/.local/android/sdk
