#!/bin/zsh

# if [ -z "$DISPLAY" ] && [ $(tty) = /dev/tty1 ]; then
if [ "$(fgconsole 2> /dev/null)" = 1 ]; then
  if [ -f "$HOME/.xinitrc" ]; then
    startx
    logout
  fi
  # (startx &); logout
  # (startx xfce &); logout
fi

# #export NODE_PATH=`npm -g root`
# eval "$(npm completion 2>/dev/null)"

# export GOPATH=$HOME/.local/go

# export PIP_DOWNLOAD_CACHE=$HOME/.pip/cache
# if [ ! -d $PIP_DOWNLOAD_CACHE ]; then
#   mkdir -p $PIP_DOWNLOAD_CACHE
# fi

# export RUBY_BUILD_SKIP_MIRROR=1
# export RUBY_BUILD_CACHE_PATH=$HOME/.rbenv/cache
# if [ ! -d $RUBY_BUILD_CACHE_PATH ]; then
#   mkdir -p $RUBY_BUILD_CACHE_PATH
# fi

# path=(
#   "$HOME/.local/opt/scala/bin"
#   "$HOME/.local/opt/sbt"
#   "$HOME/.local/opt/play"
#   "$HOME/.local/opt/akka/bin"
#   "$HOME/.local/go/bin"
#   $path
# )
# export PATH

# export GIT_PROXY_HOST='127.0.0.1'
# export GIT_PROXY_PORT='7071'

# export http_proxy="http://127.0.0.1:7051"
# export https_proxy="$http_proxy"

# export GOPATH=$HOME/.local/go
# export GOROOT=/usr/lib/go

# sbt options
# export SBT_OPTS='-Xms128m -Xmx512m -Xss1m -XX:+CMSClassUnloadingEnabled -XX:MaxPermSize=256m -XX:ReservedCodeCacheSize=192m'
#######  for maven2/ivy2 proxies
##  ~/.sbt/repositories
##    [repositories]
##      local
##      my-ivy-proxy-releases: http://repo.company.com/artifactory/ivy-releases/, [organization]/[module]/(scala_[scalaVersion]/)(sbt_[sbtVersion]/)[revision]/[type]s/[artifact](-[classifier]).[ext]
##      my-maven-proxy-releases: http://repo.company.com/artifactory/maven-releases/
##  sbt options for dropping all configured repos `sbt -Dsbt.override.build.repos=true`
##  repos:
##      jcenter         https://jcenter.bintray.com
##      central         https://repo.maven.apache.org/maven2
##      typesafe        http://repo.typesafe.com/typesafe/ivy-releases/
##      sbt-plugin      http://dl.bintray.com/sbt/sbt-plugin-releases/
##    (check `Suppress POM Consistency Checks` in artifactory for maven repos)

source $HOME/.dotfiles/zshrc

