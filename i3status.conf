
general {
        colors = true
        interval = 1
}

order += "battery 0"
order += "time"

battery 0 {
        format = "%status %percentage %remaining"
}

time {
        format = "%a %b %d %H:%M:%S"
}

