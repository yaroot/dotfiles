
general {
        colors = true
        interval = 1
}

order += "battery 0"
order += "tztime local"

battery 0 {
        format = "%status %percentage %remaining"
}

tztime local {
        format = "%a %b %d %H:%M:%S"
}

